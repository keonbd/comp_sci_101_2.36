import math
current_price = int(input())
last_months_price = int(input())

''' Type your code here. '''
monthly_morg = (current_price * 0.051) / 12
change = (current_price - last_months_price)
print(f'This house is $'+str(current_price)+'. The change is $'+str(change)+' since last month.')
print('The estimated monthly mortgage is $'+str("{0:.2f}".format(monthly_morg))+'.')
